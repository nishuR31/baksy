export const metadata = {
  title: 'Baksy',
  description: 'Baksy is your prod ready backend scaffolding buddy.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
